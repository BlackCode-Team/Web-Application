<form method="post" action="{{ path('app_vehicule_delete', {'idvehiucle': vehicule.idvehiucle}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicule.idvehiucle) }}">
    <button class="btn">Delete</button>
</form>
