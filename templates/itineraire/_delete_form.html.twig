<form method="post" action="{{ path('app_itineraire_delete', {'iditineraire': itineraire.iditineraire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ itineraire.iditineraire) }}">
    <button class="btn m-auto d-block float-right btn-full">Delete</button>
</form>
