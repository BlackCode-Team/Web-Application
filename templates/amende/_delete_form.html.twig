<form method="post" action="{{ path('app_amende_delete', {'idamende': amende.idamende}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amende.idamende) }}">
    <button class="btn">Delete</button>
</form>
